%tr{:data => {:model_id => model.id, :size_id => size.id}}
  %td
    %strong= model.full_name
  %td= format_size_attribute(size.frame_size)
  - Size::GEOMETRY_ATTRIBUTES.each do |attribute|
    %td= format_size_attribute(size.send(attribute), attribute)
